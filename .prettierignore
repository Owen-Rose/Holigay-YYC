# Build output
.next/
out/
build/
dist/
coverage/

# Dependencies
node_modules/

# Lockfiles
package-lock.json

# Generated — regenerated by `npm run db:types*`. Formatting this file would make
# it drift from the Supabase generator's output on every regeneration, and spec 006
# (FR-013) requires it stay byte-identical to what the generator emits.
src/types/database.ts

# Supabase local artifacts
supabase/.temp/

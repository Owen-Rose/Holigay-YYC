/**
 * User-facing warning shown when an application was saved but its confirmation
 * email could not be sent. Shared by the legacy and dynamic submit actions so
 * both forms say the same thing.
 */
export const EMAIL_SEND_FAILED_WARNING =
  'Application submitted, but the confirmation email could not be sent.';
